package items.properties;

public interface Readablel { // Readable esta ocupado para strings y es insoportable, por eso esta escrito
								// asi
	// Lo mismo pasa con clazz y class
	public String read(boolean visible);
}
